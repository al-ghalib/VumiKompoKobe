# Backend Environment Variables
# Copy this file to .env and configure for your deployment

# Server Configuration
# For local development: HOST=127.0.0.1
# For Render: HOST=0.0.0.0 (Render sets PORT automatically via $PORT)
HOST=0.0.0.0
PORT=8000

# CORS Configuration
# In production, replace with your frontend URL(s)
# Multiple origins can be comma-separated
# Example: https://your-frontend.vercel.app,https://your-domain.com
CORS_ORIGINS=http://localhost:3000

# Debug mode (set to False in production)
DEBUG=True

# ================================
# RENDER DEPLOYMENT NOTES:
# ================================
# 1. Create a new Web Service on Render
# 2. Connect your GitHub repository
# 3. Set Root Directory to: backend
# 4. Build Command: pip install -r requirements.txt
# 5. Start Command: uvicorn main:app --host 0.0.0.0 --port $PORT
# 6. Add environment variable in Render dashboard:
#    - CORS_ORIGINS: https://your-frontend-url.vercel.app
#    - DEBUG: False
# ================================
